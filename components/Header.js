'use client';
import { useEffect,useState } from 'react';
import { Menu, X, ArrowUpRight } from 'lucide-react';
import Link from 'next/link';
export default function Header({dark=false}){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>30);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={`header ${scrolled?'is-scrolled':''} ${dark?'header-dark':''}`}><Link href="/" className="brand" aria-label="Página inicial"><span className="brand-mark">FB</span><span><b>Fernando Borges</b><small>Urologia • Cirurgia Robótica</small></span></Link><nav className={open?'open':''}><Link href="/#sobre" onClick={()=>setOpen(false)}>Sobre</Link><Link href="/#tratamentos" onClick={()=>setOpen(false)}>Tratamentos</Link><Link href="/#estrutura" onClick={()=>setOpen(false)}>Estrutura</Link><Link href="/#contato" onClick={()=>setOpen(false)}>Contato</Link><a className="nav-cta" href="https://wa.me/556696457535?text=Olá%2C%20gostaria%20de%20agendar%20uma%20consulta." target="_blank">Agendar consulta <ArrowUpRight size={15}/></a></nav><button className="menu" onClick={()=>setOpen(!open)} aria-label="Abrir menu">{open?<X/>:<Menu/>}</button></header>}
