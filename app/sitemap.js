import { treatments } from '@/lib/data';
import { articles } from '@/lib/articles';
export default function sitemap(){const base='https://fernando-borges-urologia.vercel.app',lastModified=new Date('2026-08-13');const pages=[{url:base,changeFrequency:'weekly',priority:1},{url:`${base}/sobre`,changeFrequency:'monthly',priority:.9},{url:`${base}/conteudos`,changeFrequency:'weekly',priority:.8},{url:`${base}/politica-de-privacidade`,changeFrequency:'yearly',priority:.3}];return [...pages.map(p=>({...p,lastModified})),...treatments.map(t=>({url:`${base}/tratamentos/${t.slug}`,lastModified,changeFrequency:'monthly',priority:.85})),...articles.map(a=>({url:`${base}/conteudos/${a.slug}`,lastModified:new Date(a.date),changeFrequency:'monthly',priority:.75}))]}
